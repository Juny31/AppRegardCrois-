[build]
builder = "nixpacks"
buildCommand = "cd dashboard-collaboratif-backup/backend && npm install"

[deploy]
startCommand = "cd dashboard-collaboratif-backup/backend && npm start"
restartPolicyType = "on_failure"

[variables]
NODE_ENV = "production"